name: Snowflake Model Auto-Sync + Release (Notebook Execution)

on:
  repository_dispatch:
    types: [model_sync]
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-snowflake-model:
    runs-on: ubuntu-latest
    name: Fetch, Commit & Release Model from Snowflake (Notebook Mode)

    env:
      SNOWFLAKE_CONNECTIONS_DEFAULT_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_USER: ${{ secrets.SNOWFLAKE_USER }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
      SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Install Snowflake CLI (modern v3) and jq
      - name: Install Snowflake CLI (v3) and jq
        shell: bash
        run: |
          echo "🐍 Installing modern Snowflake CLI..."
          python3 -m pip install --upgrade pip
          pip install --user --upgrade "snowflake-cli>=3.10.0"
          sudo apt-get install -y jq
          echo 'export PATH=$HOME/.local/bin:$PATH' >> $GITHUB_ENV
          source $GITHUB_ENV
          snow --version

      # 3️⃣ Configure Snowflake connection (Password Auth)
      - name: Configure Snowflake connection
        shell: bash
        run: |
          echo "🧩 Creating ~/.snowflake/config.toml..."
          mkdir -p ~/.snowflake
          {
            echo "[connections.default]"
            echo "account = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_ACCOUNT}\""
            echo "user = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_USER}\""
            echo "password = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_PASSWORD}\""
            echo "role = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_ROLE}\""
            echo "warehouse = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_WAREHOUSE}\""
            echo "database = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE}\""
            echo "schema = \"${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA}\""
          } > ~/.snowflake/config.toml
          chmod 600 ~/.snowflake/config.toml
          echo "✅ Config file created and secured."

      # 4️⃣ Verify Snowflake connection
      - name: Verify Snowflake connection
        shell: bash
        run: |
          echo "🔎 Testing Snowflake connection..."
          snow connection list
          snow sql -q "SELECT CURRENT_USER(), CURRENT_ROLE(), CURRENT_DATABASE(), CURRENT_SCHEMA();" --format CSV

      # 5️⃣ Execute Snowflake Notebooks directly
      - name: Execute Snowflake ML Notebooks
        shell: bash
        run: |
          echo "🚀 Executing ML Notebooks directly in Snowflake..."

          # Helper function to run a notebook and check status
          run_notebook() {
            local notebook_name=$1
            echo "🧠 Running notebook: $notebook_name ..."
            snow notebook run "${notebook_name}" --wait --output json > result.json || {
              echo "❌ Failed to execute notebook: $notebook_name"
              cat result.json
              exit 1
            }

            # Display execution result summary
            cat result.json | jq '.'
            local status=$(jq -r '.[0].status' result.json)
            if [[ "$status" != "SUCCESS" ]]; then
              echo "❌ Notebook ${notebook_name} failed (status: $status)"
              exit 1
            fi
            echo "✅ Notebook ${notebook_name} executed successfully!"
          }

          # Run all four notebooks sequentially
          run_notebook "DATA_INGESTION"
          run_notebook "FEATURE_ENGINEERING"
          run_notebook "MODEL_TRAINING"
          run_notebook "MODEL_EVALUATION"

      # 6️⃣ List ONNX models in stage
      - name: List ONNX models in Snowflake stage
        shell: bash
        run: |
          echo "📜 Listing ONNX models..."
          snow sql -q "
            USE DATABASE ${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE};
            USE SCHEMA ${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA};
            LIST @${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE}.${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA}.ML_MODELS_STAGE PATTERN='.*.onnx';
          " --format table

      # 7️⃣ Download newest ONNX model and push to repo + release
      - name: Download latest ONNX file, commit, and release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          echo "📥 Fetching latest ONNX file..."
          mkdir -p staged_models
          json_out=$(snow sql -q "
            USE DATABASE ${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE};
            USE SCHEMA ${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA};
            LIST @${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE}.${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA}.ML_MODELS_STAGE PATTERN='.*.onnx';
          " --format json)
          latest_file=$(echo "$json_out" | jq -r '
            flatten
            | map(select(.name? != null))
            | sort_by(
                ( .last_modified
                  | sub(", "; " ")
                  | strptime("%a %d %b %Y %H:%M:%S GMT")
                  | mktime )
              )
            | reverse
            | .[0].name
          ')
          if [ -z "$latest_file" ] || [ "$latest_file" == "null" ]; then
            echo "❌ No ONNX files found!"
            echo "$json_out" | head -n 40
            exit 1
          fi
          echo "✅ Latest file: $latest_file"
          clean_file=${latest_file#ml_models_stage/}
          echo "🧩 Downloading model..."
          snow sql -q "GET @${SNOWFLAKE_CONNECTIONS_DEFAULT_DATABASE}.${SNOWFLAKE_CONNECTIONS_DEFAULT_SCHEMA}.ML_MODELS_STAGE/$clean_file file://staged_models/"
          echo "✅ Model downloaded to staged_models/$clean_file"

          # Commit model to repo
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add staged_models/
          git commit -m "Auto-sync ONNX model from Snowflake stage ($clean_file)" || echo "No new model changes"
          CURRENT_BRANCH="${GITHUB_REF_NAME}"
          git pull --rebase origin "${CURRENT_BRANCH}" || true
          git push origin "${CURRENT_BRANCH}"

          # Create a release
          RELEASE_TAG="model-$(date +'%Y%m%d-%H%M%S')"
          LATEST_MODEL_FILE=$(basename "$clean_file")

          gh release create "$RELEASE_TAG" "staged_models/$LATEST_MODEL_FILE" \
            --title "Model Release ($RELEASE_TAG)" \
            --notes "Latest trained model automatically synced from Snowflake." \
            --latest

      # 8️⃣ Summary
      - name: Summary
        run: |
          echo "✅ All notebooks executed and latest model released successfully!"
